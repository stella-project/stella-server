version: '3'
name: stella-server-dev

services:
  stella-server:
    build: ./web
    expose:
      - "8000"
    ports:
      - "8000:8000"
    environment:
      - FLASK_APP=app/app
      - FLASK_CONFIG=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PW=change-me
      - POSTGRES_DB=postgres
      - SECRET_KEY=change-me
      - AUTOMATOR_GH_KEY=
      - ADMIN_MAIL=admin@stella-project.org
      - ADMIN_PASS=pass
      - GESIS_MAIL=gesis@stella-project.org
      - GESIS_PASS=pass
      - LIVIVO_MAIL=livivo@stella-project.org
      - LIVIVO_PASS=pass
      - PARTA_MAIL=participant_a@stella-project.org
      - PARTA_PASS=pass
      - PARTB_MAIL=participant_b@stella-project.org
      - PARTB_PASS=pass
    command: bash -c "flask seed-db &&
              flask db init &&
              flask db migrate &&
              flask db upgrade &&
              gunicorn -w 2 --timeout 60 -b :8000 'app.app:create_app()'"
    links:
      - stella-server-db:db
    depends_on:
      - stella-server-db
  stella-server-db:
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=change-me
      - POSTGRES_DB=postgres
  # nginx:
  #   restart: always
  #   build: ./nginx
  #   ports:
  #     - 80:80
  #     - 443:443
  #   volumes:
  #     - /etc/ssl:/etc/ssl
  #     - /etc/letsencrypt/live:/etc/letsencrypt/live
  #     - /etc/letsencrypt/archive:/etc/letsencrypt/archive
  #   links:
  #     - web:web
  #   depends_on:
  #     - web
